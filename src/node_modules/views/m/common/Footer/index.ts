import * as tomato from "@po-to/tomato";
import * as tdom from "@po-to/tomato-jquery";
import * as project from "views/global/common/Project";
import * as funs from "views/global/common/Funs";
import about = require("views/m/about/AboutForm");

class VPresenter extends project.VPresenter{
    constructor(view: tomato.VPView, parent?: tomato.VPresenter, vpid?:string){
        super(view,parent,vpid);
        this._watchEvent();
    }
    _evt_openAboutPage(data,target,hit){
        tomato.getVPresenter<about>(funs.moduleToUrl('m/about/AboutForm'), (about) => {
            //弹出
            tomato.application.appendChild(about);
            var point = $(target).attr('href');
            about.scrollTo(point);
        })
    }
}
export = VPresenter;